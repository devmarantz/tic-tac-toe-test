const { validateMove, getValidMoveFromPlayer } = require('../lib/move');

describe('validateMove', () => {
  it.todo('should turn the move string into a tuple', () => {});
  it.todo('should throw an error if input has incorrect syntax');
  it.todo('should throw an error if the move is out of bounds');
  it.todo('should throw an error if the space is already occupied');
});

describe('getValidMoveFromPlayer', () => {
  it.todo('should ');
});
